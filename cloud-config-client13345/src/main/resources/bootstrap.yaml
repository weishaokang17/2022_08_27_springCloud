server:
  port: 13345


#服务名称
spring:
  application:
    name: config-client

  cloud:
    #Config客户端配置
    config:
      #config 客户端配置
      label: master
      name: config-dev,config-prd,config-test
      #profile: dev
      #上述3个综合：master分支上jdbc-prd.yml的配置文件被读取 http://localhost:13344/master/jdbc-prd.yml
      uri: http://localhost:13344
    #cloud[1]:
      #config 客户端配置
      #label: master
     # name: jdbc
      #profile: prd
      #上述3个综合：master分支上jdbc-prd.yml的配置文件被读取 http://localhost:13344/master/jdbc-prd.yml
      #uri: http://localhost:13344





#服务注册到eureka地址
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka,http://eureka7003.com:7003/eureka